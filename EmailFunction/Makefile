# Copy pre-built function to SAM artifact dir (no npm install during build).
# Run "npm install" in EmailFunction once, then "sam build" will only copy.
build-EmailFunction:
	cp -r . $(ARTIFACTS_DIR)/
